 
 
def build_docker_image(name:str, dockerfile:str, visibility=None):
    
    tag = "%s:latest"%(name)
    command = "docker build -t %s %s" % (tag, dockerfile)

    
    return build_rule(
        name = name,
        cmd = command,
    )