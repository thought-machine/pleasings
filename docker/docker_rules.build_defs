 
 
def build_docker_image(name:str, dockerfile:str, visibility=None):
    
    command = 'docker build -t please_test:latest -f ' + dockerfile

    
    build_rule(
        name = name,
        cmd = command,
    )