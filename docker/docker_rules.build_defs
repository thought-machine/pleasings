 
 
def build_docker_image(name:str, dockerfile:str, srcs, visibility=None, out:str=None):
    
    tag = "%s:latest"%(name)
    log.info("Building docker image for %s" % tag)
    output = "%s.log" %(tag)
    log.info("base is")
    log.info("pkg is")
    pkg = '$PKG'
    log.info(pkg)
    log.info("pkg dir is")
    pkg_dir = '$PKG_DIR'
    log.info(pkg_dir)
    
    command = "docker build --no-cache -t  %s $SRCS > %s " % (tag, output)
    
    build_rule(
        name = name,
        cmd = command,
        building_description='building docker_image...',
        output_is_complete=True,
        outs = [output],
        visibility = visibility
    )