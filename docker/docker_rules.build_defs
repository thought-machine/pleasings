 
 
def build_docker_image(name:str, dockerfile:str, visibility=None):
    
    command = 'docker -t please_test:latest build -f ' + dockerfile

    
    build_rule(
        name = name,
        cmd = command,
    )