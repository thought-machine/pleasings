 
 
def build_docker_image(name, dockerfile, visibility=None):
    dockerfile = $SRCS_RESOURCES[0]

    command = 'docker build {}'.format(dockerfile)
    
    build_rule(
        name = name,
        cmd = command,
    )